# Decision Log: BmadPoc Search Application Lab

This log tracks all key product and architecture decisions, overrides, and changes made during the PRD process.

## Log of Decisions

| Date | ID | Decision / Change | Rationale | Status |
| :--- | :--- | :--- | :--- | :--- |
| 2026-06-04 | DEC-001 | Initialization | Initialized PRD workspace for BmadPoc Search Application Lab. | Approved |
| 2026-06-04 | DEC-002 | CSV Read on Request | The backend will read and parse the CSV file on every search request rather than caching it in memory on startup. This allows developers to edit `users.csv` during the lab and see changes instantly without restarting the backend. | Approved |
| 2026-06-04 | DEC-003 | Local `serve` Dependency | Include `serve` as a local dependency in `sg-search/package.json` and run it via `npm start` (which executes `serve . -p 3000`). This ensures zero-config local execution without global permissions. | Approved |
| 2026-06-04 | DEC-004 | `csv-parser` Library | Use the standard `csv-parser` npm package for robust CSV parsing, including handling quotes and edge cases. | Approved |
| 2026-06-04 | DEC-005 | Finalization | Finalized and closed the PRD for LAB-03: Full-Stack Search Application Hands-On Lab. | Approved |
