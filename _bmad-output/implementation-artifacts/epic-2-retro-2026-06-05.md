# Epic 2 Retrospective — Facilitator Workshop Readiness

**Date:** 2026-06-05  
**Project:** AI_POC_Lab4  
**Epic:** 2 — Facilitator Workshop Readiness  
**Facilitator:** Amelia (Senior Software Engineer)  
**Project Lead:** SG_Engineer_Aman  
**Status:** Complete

---

## Epic Summary

| Metric | Value |
|--------|-------|
| Stories completed | 2 / 2 (100%) |
| FRs covered | FR-10, FR-11, FR-12 |
| Primary deliverables | `LAB-03-Search-App-Guide.md`, `setup-lab.ps1`, `verify-lab.ps1` |
| NFR-4 (four-artifact consistency) | Achieved after retro cleanup |
| Production incidents | 0 |
| Technical debt deferred | 4 (workshop-scope, low priority) |

### Stories Delivered

1. **2.1** — Lab Guide MongoDB Workflow  
2. **2.2** — Workshop Setup & Verification Scripts  

### Team Participants

- John (Product Manager)
- Winston (System Architect)
- Amelia (Senior Software Engineer)
- Mary (Business Analyst)
- Paige (Technical Writer)
- Sally (UX Designer)
- Dana (QA Engineer)
- Charlie (Senior Dev)
- SG_Engineer_Aman (Project Lead)

---

## What Went Well

**Project Lead assessment:** Everything went well in Epic 2 — no major delivery concerns raised.

### Success Themes

1. **Four-artifact MongoDB workflow alignment (NFR-4)** — README, lab guide, setup script, and verify script consistently describe Atlas + auto-seed; no CSV runtime-loading instructions remain.
2. **Story 2.1 guide rewrite strategy** — Grep validation gates (forbidden/required phrases) worked well for a documentation-only story; ~150 lines of greenfield CSV `server.js` tutorial removed.
3. **`Test-MongodbEnv` pattern (Story 2.2)** — Warn-only readiness check; never echoes URI; runs before `npm install`; deps still install when `.env` is missing.
4. **Frozen verify test matrix (SM-1)** — `verify-lab.ps1` expected counts (1, 3, 400) unchanged; Story 2.2 limited changes to troubleshooting hint strings.
5. **Cross-epic handoff from Epic 1** — Story 2.1 explicitly deferred script work to 2.2; README from Story 1.5 served as authoritative reference.
6. **Epic 1 retro cleanup completed during this retro** — Batches A, B, and C closed outstanding facilitator/cohort risks (see below).

---

## Challenges & Growth Areas

### Common Struggles

| Pattern | Stories Affected | Impact |
|---------|------------------|--------|
| Cross-story scope boundaries | 2.1, 2.2 | 2.1 deferred scripts; 2.2 still edited lab guide for NFR-4 alignment |
| Epic 1 retro items not picked up in Epic 2 stories | 2.1, 2.2 | Node engine mismatch and PS 5.1 400 test left open until retro cleanup |
| Pre-existing doc/script mismatches | 2.1 review, 2.2 review | CORS check claim, README auto-seed wording, frontend npm install inconsistency |
| `.env` parsing edge cases | 2.2 | BOM, empty lines, unreadable file — 7 code-review patches applied |

### Recurring Deferred Items (Workshop-Scope)

- Guide vs `setup-lab.ps1` on frontend `npm install` when `sg-search/package.json` exists
- Guide vs `setup-lab.ps1` on `users.csv` recovery (setup auto-creates; guide says restore from repo)
- Atlas AC labeling in PM story templates (Batch D — optional)
- Workshop-scope security: unrestricted CORS, no auth/rate limiting, no `/health` DB readiness signal

### Key Insights

1. **Retrospectives should track cross-epic action items** — Epic 1 commitments (Node engines, verify script) were not in Epic 2 story ACs; they slipped until explicitly revisited.
2. **Documentation stories need explicit "out of scope" grep gates** — Prevents guide claiming behavior the verify script does not implement.
3. **PS 5.1 HTTP client choice matters** — `Invoke-WebRequest` swallows 4xx bodies; `HttpWebRequest` is required for reliable error JSON parsing.

---

## Previous Epic Retrospective Follow-Through (Epic 1)

| Epic 1 Action Item | Status After Epic 2 + Cleanup |
|--------------------|-------------------------------|
| Document Atlas-dependent ACs in story templates | ⏳ Partial — stories label ACs well; PM template not updated |
| Node ≥20.19.0 in lab guide | ✅ Done (Batch A, retro session) |
| Update `package.json` engines | ✅ Done (Batch A) |
| `verify-lab.ps1` PS 5.1 HTTP 400 fix | ✅ Done (Batch B) |
| Shared Atlas pollution guard | ✅ Documented in verify hints (Story 2.2) |
| Lab guide MongoDB rewrite | ✅ Done (Story 2.1) |
| Setup/verify script alignment | ✅ Done (Story 2.2) |
| Run `verify-lab.ps1` as validation gate | ✅ Done |

---

## Post-Retro Cleanup (Batches A, B, C)

Executed during Epic 2 retrospective at Project Lead direction.

### Batch A — Cohort blockers

- `sg-search-service/package.json`: `engines.node` → `>=20.19.0`
- `LAB-03-Search-App-Guide.md`: prerequisites → Node.js 20.19.0+
- `setup-lab.ps1`: `Test-NodeVersion` warns if below 20.19.0 (warn-only, same pattern as `Test-MongodbEnv`)
- `sg-search-service/README.md`: prerequisites aligned with engines

### Batch B — Verify script trust

- `verify-lab.ps1`: `Invoke-LabGet` rewritten to use `[System.Net.HttpWebRequest]` for PS 5.1 4xx body parsing
- Verified: all 4 API checks PASS (health, John+Smith count=1, firstName=John count=3, no-params HTTP 400)

### Batch C — Doc accuracy

- `sg-search-service/README.md`: auto-seed described as idempotent upsert every startup (not "if collection empty")
- `LAB-03-Search-App-Guide.md`: removed false CORS header check claim in verify-lab section

---

## Next Epic Preview

**Epic 3:** Not defined in `epics.md`. The MongoDB migration roadmap (Epics 1–2) is complete.

**Workshop readiness:** Lab is facilitator-ready for next cohort after retro cleanup.

---

## Action Items

### Process Improvements

1. **Add Atlas-dependent AC note to PM story template**  
   - Owner: John (Product Manager)  
   - Priority: Low  
   - Success criteria: Story ACs distinguish "code-verifiable locally" vs. "requires facilitator `.env`"

2. **Review deferred-work.md before next epic/planning cycle**  
   - Owner: Amelia (Developer)  
   - Success criteria: Low-priority deferred items triaged or explicitly accepted for workshop scope

### Technical Debt (Remaining, Low Priority)

1. **Guide vs setup-lab frontend npm install wording**  
   - Owner: Paige (Technical Writer)  
   - Priority: Low  

2. **Guide vs setup-lab users.csv recovery wording**  
   - Owner: Paige (Technical Writer)  
   - Priority: Low  

### Team Agreements

- Run `verify-lab.ps1` (all 4 API checks PASS) before marking facilitator-facing stories complete
- Close Epic 1 retro action items in the same sprint as Epic 2 delivery — do not defer to "later"
- Maintain warn-only pattern for facilitator readiness checks (`.env`, Node version) — never echo secrets
- Use `HttpWebRequest` (not `Invoke-WebRequest`) for PS 5.1 scripts that parse error response bodies

---

## Readiness Assessment

| Dimension | Status | Notes |
|-----------|--------|-------|
| Testing & Quality | ✅ Good | `verify-lab.ps1` all 4 API checks PASS; `npm test` passes with `.env` |
| Deployment | ⚠️ Local/dev | Workshop lab environment |
| Stakeholder Acceptance | ✅ Positive | Project Lead: Epic 2 delivery successful |
| Technical Health | ✅ Stable | Epic 1 retro gaps closed in cleanup batches |
| Unresolved Blockers | None | Ready for next workshop cohort |

**Verdict:** Epic 2 is complete and workshop-ready. MongoDB migration project (Epics 1–2) delivered.

---

## Key Takeaways

1. **Epic 2 closed the facilitator loop** — Participants (Epic 1) and facilitators (Epic 2) now have aligned MongoDB workflow artifacts.
2. **Cross-epic retro follow-through is essential** — Epic 1 action items that were not in Epic 2 story scope slipped until explicitly revisited in this retro.
3. **PowerShell 5.1 compatibility requires explicit testing** — A passing status code is not enough; error body parsing must be verified on the target shell.

---

## Commitments

| Category | Count |
|----------|--------|
| Action items | 2 |
| Remaining low-priority debt | 2 |
| Cleanup batches completed | 3 (A, B, C) |

---

## Next Steps

1. Review this retrospective summary  
2. Confirm facilitator Atlas URI and IP allowlist before next cohort (Project Lead)  
3. Run full lab dry-run: `setup-lab.ps1` → `npm start` → `verify-lab.ps1`  
4. Triage optional Batch D (PM template) if planning future epics  

---

*Retrospective facilitated per BMad bmad-retrospective workflow.*
